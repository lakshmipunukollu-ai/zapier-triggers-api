[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd app && pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
